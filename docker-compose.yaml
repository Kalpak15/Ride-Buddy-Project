version: '3.9'

services:

  backend:
    image: kalpak15/ride_buddy_backend
    ports:
      - 3000:3000
    env_file:
      - ./Carpooling_Backend/.env

  frontend:
    build:
     context: ./Carpooling_Frontend
     args:
      VITE_key_id: rzp_live_tmijbiWVasK15p
      VITE_key_secret: ETzD4BJudFEyN8XbgmKlWLkd
      VITE_API_URL: http://backend:3000

    ports:
      - 5173:5173

  
